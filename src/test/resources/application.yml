spring:
  datasource:
    url: jdbc:h2:mem:testdb   # 메모리에서 실행 (데이터 사라짐)
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update

  main:
    allow-bean-definition-overriding: true

aws:
  s3:
    region: test
    secret-key: test
    access-key: tesst
    bucket-name: test